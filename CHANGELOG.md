# Changelog

每个版本只记「改了什么」。实现取舍、验证数据与遗留项在 `docs/` 与 `docs/STATUS.md`。

## SunnyTV 0.1.0-dev27

- 详情页默认聚焦到第一个「继续播放 / 播放」按钮；按上回到顶部导航、再按下回到该按钮。
- 排序选择窗口改为多行多列的固定网格，不用翻页即可选；排序按钮左侧改为等高的上下箭头表示方向；
  比特率与大小改为在设备端按文件码率 / 体积排序（Emby 的 item 接口会忽略这两个排序键，实测见文档）。
- 播放器快进 / 快退图标改为双三角并去掉秒数角标；睡眠图标重绘为新月。
- 搜索页把字母数字键盘移到搜索栏上方并整体居中，退格与清除放在字母与数字之间。
- 「视图」按钮单击即在海报 / 背景 / 横幅之间循环，不再进入子菜单。
- 修复「裁切」画面裁得过多：只按屏幕比例挤掉多余黑边，不再额外放大 12%。
- STRM 直连打不开时改用 Emby 服务端读取一次；HTTP 409 提示指向失效直链或令牌。

## SunnyTV 0.1.0-dev26

- 播放传输单独放宽首次响应预算（连接 15 秒 / 读取 60 秒），API、图片与 STRM 文本仍为 8 / 25 秒。
- 首帧前遇到瞬时传输失败自动重试一次；DNS、TLS、HTTP 4xx 与解析错误不重试。
- 播放诊断改为只显示主机与端口，并标注失败阶段与重试次数。
- HTTP 409 与设置页直接播放测试不再特指 MediaIndex。

## SunnyTV 0.1.0-dev25

- 公开发布：去掉开发期临时内置的第三方字体，仓库与安装包不含任何字体文件。
- 手机 / 平板媒体库与文件夹视图里 banner 与媒体区的多余留白从 122dp 收到 40dp（电视值不变）。
- 清掉 dev22 起累积的 lint 报错（ICU 转写 API 守卫、Media3 opt-in、播放器按键抑制）。

## SunnyTV 0.1.0-dev24

- 媒体库从媒体区按「上」回 banner 改为单段滚动，去掉中途停顿与二次补偿。

## SunnyTV 0.1.0-dev23

- 聚焦外框改为与卡片轮廓同心，修掉圆角处的细缝。
- 首页与媒体库行内预留柔和阴影留白，最左卡片与媒体块的阴影不再被裁掉。

## SunnyTV 0.1.0-dev22

- 手机 / 平板改为单一可触摸滚动结构；媒体库推荐栏与首页改成同一套「一大块 + 小条块」轮播。
- 播放进度条常显进度点；电视可聚焦进度条后长按左右连续快进快退。
- 长按确定 / 长按触摸打开刷新与删除菜单（删除需二次确认）。
- 字体并入外观，新增字幕外观（字体、大小、描边、位置、背景）。

## SunnyTV 0.1.0-dev21

- 首页 Hero 占满视口，与「我的媒体库」首屏同时预布局，下翻只做 GPU 平移。
- 播放器快退 / 快进改为环形箭头并显示秒数；倍速按钮只显示数值。

## SunnyTV 0.1.0-dev20

- 媒体库工具栏改为闲置圆形图标、聚焦后展开文字；排序按钮按字段更换图标并叠加升降序提示。
- 海报墙按服务端 total 建立虚拟槽位，移除焦点链里的「加载更多」按钮。
- 播放器 OSD 改为「最后一次操作后的空闲计时」，并重绘传输、倍速与睡眠图标。

## SunnyTV 0.1.0-dev19

- 媒体库标题与工具按钮合并成单行透明工具栏。
- 新增界面显示模式偏好（自动 / 1080p / 原生最高）与显示诊断。
- 播放器控制改为轻量矢量与半透明玻璃风格。

## SunnyTV 0.1.0-dev16

- 网格跨行焦点统一走共用导航器，工具栏可下移进入海报墙。
- 首页每个媒体库增加稳定的排序按钮（最新入库 / 最新上映 / 随机）。
- 公开签名改用仓库既有 Secret，取消 dev14 证书限制。

## SunnyTV 0.1.0-dev13

- 浅色焦点改为柔和阴影 + 聚焦框，深色保持无阴影与更粗的聚焦框。
- 播放器按钮与控制布局重置，补充章节、字幕、音轨、演职员、睡眠、跳过提示与下一集提示。
- 纵向 4K MP4 播放问题经用户实机确认已修复。

## SunnyTV 0.1.0-dev12

- 首页焦点路径、焦点阴影与描边修正。
- Media3 定向升级到 1.9.4，并为 MP4 错误 2000 增加一次忽略 edit list 的兼容重试。

## SunnyTV 0.1.0-dev11

- 深浅主题焦点描边、首页续播胶囊与设置间距调整。
- 播放器改为圆形图标控件与轨道列表，增加不含地址与凭据的错误诊断。

## SunnyTV 0.1.0-dev2

- 延续 dev1 工程，不重做或删除现有页面与来源；原生顶部增加胶囊导航与当前栏目高亮。
- 音轨 / 字幕面板打开时移除下层播放控制的可聚焦入口。
- 起播计时拆分，新增时序契约断言；新增源码初始化与 Android CI。

## SunnyTV 0.1.0-dev1

- 初始原生源码：Emby、CloudDrive2 WebDAV、STRM、安全策略与 Media3 基础实现。
