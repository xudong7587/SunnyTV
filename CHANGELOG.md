# SunnyTV dev25

- 验收第二十四轮：手机 / 平板（触摸设备）的媒体库与文件夹视图里，banner 与媒体区之间的空隙太大。量测后确认 122dp 的构成为 18dp（banner 底边距）+ 22dp（网格项间距）+ 82dp（工具行上内边距），其中 82dp 是电视端"整屏切换"必须的顶栏内边距——电视把工具行整行对齐到视口顶端，靠它把标题与按钮留在顶栏下方；触摸设备自由滚动，这段就成了空白。现在触摸设备改用 8dp 上内边距（电视仍取 `pageTopPadding`），`LibraryHero` 底部留白触摸设备 18→10dp（电视不变）；`enterTools()` 与文件夹返回的落位偏移改为 `toolsTopInsetPx`，电视上逐值等于原来的 `pinnedTop`。结果：手机横屏 / 竖屏 122dp → 40dp，电视保持 122dp。新增 `BannerMediaGapTest` 同时断言这两个数字。详见 `docs/UI-DEV25.md`。

# SunnyTV dev24

- 验收第二十三轮：媒体库（含文件夹内部）从媒体区按「上」回 banner 时中间会卡一下。逐帧量测确认返回过程是**两段动画**：先按当前可见首行高度滚一次、速度衰减到完全停住，再重新加速走完剩下的距离。根因是 `GridFocusNavigator.move(0)` 走的 `revealItem` 在目标项不在可见列表时只能逐行滚动，而 banner 是占满视口的整项。现在改为与"banner → 媒体区"同一套写法：新增 `leaveToolsForBanner()`，一次 `animateScrollToItem(0)` 回到 banner 自己的偏移、焦点并行请求、全程 hold 住视口；`beforeMove(0)` 同样改为单次滚动；文件夹模式从文件夹行回到工具行也改为一次 `animateScrollToItem(1, pageTopPadding)` 落位。新增 `BannerReturnMotionTest` 逐帧采样并断言"只走一段动画、返回后 banner 位置与进入前一致"（同一测试在 dev23 源码上会失败，可复现该回归）。详见 `docs/UI-DEV24.md`。

# SunnyTV dev23

- 验收第二十二轮（细节打磨）：①聚焦外框改为与卡片轮廓**同心**的内缩描边（圆角半径随内缩一起减小），修掉圆角处"角没包住"的细缝；②首页最左媒体库卡片与最左折叠媒体块的柔和阴影被行分区裁掉的问题，改为在行内预留左右 14dp / 上 12dp / 下 18dp 的阴影留白，并把外层内边距减回相同数值，标题与第一张卡的左边缘不动；媒体库工具栏按钮、详情页演员表圆头像行、云盘目录列表同步处理。新增 `FocusFrameVisualTest` 导出首页/媒体库真机截图并断言阴影存在与圆角同心。详见 `docs/UI-DEV23.md`。

# SunnyTV dev22

- 验收第二十一轮：开发期间的私有构建临时加入了第三方字体（第三个为 `汉仪元隆黑 60J`），仅用于自用验证；这些字体已在 dev25 的公开版清理中全部移除，仓库与 APK 均不含任何字体文件。
- 验收第二十轮：定位并修好三个具体缺陷——①"白方块"来自我给工具行铺的不透明底色，已移除；②工具行贴顶的偏移量算错（LazyGrid 把首项画在 contentPadding 下方，而 offset 只报内容内偏移），现按 `contentPadding + 上报偏移` 正向纠正；③首页向上聚焦"我的媒体库"时我多加的 `revealItem(0)` 在触摸布局里等于把 banner 滚回来，已删除。文件夹展开模式的落位同一轮已统一。
- 验收第十九轮：文件夹展开模式沿用同一套落位——`moveFolder` 原来按"钉住工具栏"时代的下方偏移定位，会留下 banner 边缘；现统一改为落在顶栏下方，文件夹标题/展开媒体行/更多文件夹按钮都不再露 banner。
- 验收第十八轮：两区改为整屏切换——媒体库按下后工具行整行贴顶并铺一层主题底色，banner 完全隐藏、与媒体区形成清晰分界；首页从媒体块向上聚焦"我的媒体库"时改为整页吸附（列表回第 0 项），不再只露半个 banner，再按上才回 banner 区。
- 验收第十八轮：两区改为整屏切换——媒体库按下后工具行整行贴顶并铺一层主题底色，banner 完全隐藏、与媒体区形成清晰分界；首页从媒体块向上聚焦"我的媒体库"时改为整页吸附（列表回第 0 项），不再只露半个 banner，再按上才回 banner 区。
- 验收第十七轮：按用户录屏定位——上一轮"对齐后再回退顶栏内边距"的多余一步把 banner 一条留在最上面；现改为整行 `animateScrollToItem(1)` 直接对齐到视口顶端，并让工具行自带 `top = pageTopPadding` 内边距，使标题/按钮正好落在顶栏下方、上方不再露 banner；测试补位置断言并改用 30 条数据以贴近真实库规模。
- 验收第十六轮：在 TV 模拟器复现并修复"媒体库 banner 按下键被阻断"——根因是 hero 那个 Box 上 `gridFocusTarget` 的按键处理器排在 `onPreviewKeyEvent` 之前（预览阶段由外到内），它先消费了「下」并静默失败；现调整修饰符顺序让 `enterTools()` 先接管，并用"先对齐再回退内边距 + 聚焦具体按钮"的确定性滚动完成区域切换；同步修正因工具行不再钉住而过期的测试断言。
- 验收第十五轮：按"如无必要勿增实体"移除顶部「换一批」按钮（刷新入口保留顶栏按上与 banner 下拉）；修复媒体库 banner 向下被阻断——banner 的外层按键与轮播卡片的两条"向下"入口统一走同一套受控滚动（对齐工具行到顶部内边距再交接焦点）。
- 验收第十四轮：修复轮播库列表跨来源混淆——"参与轮播的媒体库"只列出当前来源的库，且当前来源没有任何勾选时自动按该来源全部媒体库运行，避免轮播取不到候选导致"刷新无效果"。
- 验收第十三轮：撤掉 banner 内新增的「换一批」按钮并恢复原布局（它曾把手机上媒体库 banner 挤出屏幕、把首页续播胶囊顶掉）；刷新入口改为顶部钉住那一行的第 5 个按钮，刷新当前页面 banner；手动刷新保证换片——首页非随机模式轮换候选池、媒体库刷新后自动前进到下一张海报。
- 验收第十二轮：手动刷新在"最新入库/继续观看"模式下也会轮换候选池（原来重取同一列表所以看起来没变）；首页与媒体库 banner 的"向下查看更多媒体"右侧新增「换一批」按钮作为遥控器保底入口；媒体库从 banner 下移到工具行改成与首页区域切换一致的受控滚动（对齐到顶部内边距再交接焦点），消除弹跳与卡住。
- 验收第十一轮：设置 → 媒体来源新增「当前显示来源」，可切换要浏览的 Emby 来源；同一时间只显示所选来源的媒体（首页、媒体库、搜索、轮播、启动快照、删除权限检查都跟随），新添加的来源自动成为当前来源，移除当前来源时回退到剩余的第一个。
- 验收第十轮：强制只使用第一个 Emby 来源，首页、媒体库、搜索、轮播推荐、启动快照与删除权限检查全部只针对该来源，其余来源不再合并显示也不再发请求；不提供来源切换入口，设置页仅在检测到多来源时给出说明。海报缓存仍按来源 + 条目 + tag + 尺寸做键，同来源同名条目天然共用。
- 验收第九轮：修复 banner 下拉手势把向上滑动一起吃掉导致页面滚不动的问题——改为手写方向判定，只在「已到顶 + 向下拖」时消费事件，判定向上即把整个手势交还列表。
- 验收第八轮：banner 下拉刷新手势改为直接挂在 banner 上（到顶状态下按住 banner 向下拖 72dp 松手即刷新，向上仍正常滚动），并在真机验证首页轮播确实换片。
- 验收第七轮：修复电视媒体库无法按下键进入下方内容（工具行作为真实网格项先滚动再聚焦）；媒体库只在顶部导航上按上才刷新；搜索键盘改为左侧三行字母、右侧两行数字 + 退格/清除；取消左上角 logo 阴影；画面"裁切"改为等比例放大并裁掉烘焙黑边；播放缓冲提升到 30s/120s、目标缓冲按堆内存 1/8、读取块 256KB 以改善高码率 STRM 卡顿；平板纳入触摸设备判定，首页可滑动并按横竖屏切换 banner 素材；轮播随机算法加入新鲜度优先；手机与平板下拉刷新轮播生效。
- 验收第六轮：媒体库的标题/工具行不再钉住，改为 banner 之后的普通滚动项，跟随海报墙一起滚动，向上翻也不会停在顶部；"按文件夹"入口对所有媒体库开放（电影库也能用）。
- 验收第五轮：媒体库工具栏恢复"跟随滚动、到顶钉住"，占位项未进入视口时停在首屏之外，彻底消除与 banner 的重叠；焦点在工具行按上回到 banner。搜索页改为标准遥控键盘（A–Z 三行 + 0–9，纯文字无边框，行尾退格 ← 与清除垃圾桶），并新增拼音首字母顺序匹配（ICU 转写、本地匹配，输入 sdyq 命中"速度与激情"）。
- 验收第四轮：修复媒体库工具行压在 banner 上的根因（工具行不再停到视口底边）；「所有媒体库」标题随网格一起滚动；库页/详情页恢复顶部按钮与左侧 logo，顶部下移只在手机竖屏生效；logo 改用系统原生 elevation 阴影；字幕投影改为主题色浅色（描边宽度由 Media3 写死，按用户意见不做自绘渲染）；详情页在有删除权限时显示「删除」按钮；搜索页新增拼音首字母/常用字快速选择（ICU 转写，无内置辞书），键位纯文字并带退格与清除图标。
- 验收第三轮：媒体库轮播与工具栏之间固定留白（宽屏 30dp、竖屏 56dp），细条媒体块不再压住标题/工具按钮；顶部下移只在手机竖屏生效，横屏与电视恢复原高度；左上角 logo 去掉圆形阴影，只保留文字阴影；媒体库轮播改为 1 大块 + 6 细条，左键循环一轮后跳出并聚焦回左侧按钮；首页顶栏按上直接刷新轮播。
- 验收第二轮：媒体库推荐栏与首页完全同排版（宽屏按钮在左、媒体块在右且下对齐、占 58% 宽度；
  窄屏媒体块在按钮下方并留出间距）；详情页改为与媒体库页相同的固定全屏背景，只在下方渐变，
  图片完整呈现；修复首页「按到顶再按上刷新轮播」不生效的两个原因；字幕外观加入实时预览并合并
  为一张设置卡；logo/文字阴影只作用于图形与文字本身；移除手机滑动模拟方向键的代码。
- 手机（含横屏）统一走可触摸滚动的首页结构，横屏下滑可以正常拉起「我的媒体库」；电视端双层 Hero 转场保持不变。
- 媒体库上方推荐栏改为与首页相同的「一大块 + 小条块」轮播，默认随机推荐，左侧播放/详情按钮不变；媒体库与首页 Hero 的背景渐变后移，图片呈现更完整。
- 首页与媒体库顶部继续上滑/上滑到顶可刷新轮播与相关媒体，刷新时显示播放器同款品牌 logo + LOADING 动画。
- 播放进度条常显已播放圆点；电视可从控制排按上聚焦并长按左右连续快进/快退并显示标记时间；手机水平滑动取消中三分之一限制（保留边缘返回手势），并可直接拖动圆点定位。
- 「字体」并入「外观」：界面字体与字幕字体都可选系统默认或用户自定义上传；新增字幕外观（字体、大小、描边、位置、背景），颜色跟随主题。
- 首页、媒体库、海报墙支持长按确定/长按触摸打开菜单：刷新媒体库、刷新元数据、删除（删除需二次确认）。
- 设置中「媒体来源」移到「性能」下方；左上角 logo 与 SunnyTV 文字使用浅色聚焦媒体块同款阴影；手机端顶部钉住元素下移避开前置摄像头。
- 遥控器菜单键直达设置页（播放页除外）；手机在非交互区域上下左右滑动模拟电视方向键，点击与海报/媒体块滑动行为不变。
- 版本提升到 `0.1.0-dev22` / `versionCode 22`。

验证：`tests/CoreContractTest.kt` 119/119 通过（含新增字幕外观契约）；`scripts/check-project.py` 通过；Android `:app:assembleRelease` 成功并产出已签名 APK（证书与既有密钥一致），覆盖安装到用户手机后启动无异常；API 30 TV 模拟器执行 53 项仪器化测试，48 项通过，5 项为历史遗留断言/超时（详见 `docs/UI-DEV22.md`）。实体电视与手机手感仍需人工验收。

# SunnyTV dev21

- 首页 TV Hero 改为完整占满当前视口；Hero 与“我的媒体库”首屏同时预布局，切换时只做两个已渲染页面的 GPU 平移，避免下翻过程中再触发 LazyColumn 组合/图片解码；首两条媒体库内容也提前预取。
- 首页媒体行上下切换先交接焦点再滚动，媒体块展开与焦点同帧启动；同一行内移动时本地视觉选中状态与 FocusRequester 同步，缩短展开/Artwork 过渡。
- 播放器快退/快进改为逆时针/顺时针环形箭头，中间显示秒数；倍速按钮只显示 `1.0x / 1.25x / 1.5x / 2.0x` 数值，不再绘制色块。
- 播放器子菜单右上角关闭按钮保留无障碍说明，但不再显示聚焦文字提示。
- 版本提升到 `0.1.0-dev21` / `versionCode 21`。

验证：`scripts/test-core.sh` 114/114 通过；`scripts/check-project.py` 的 Android XML/Python/原生资源检查通过，仍有 dev20 基线既存的 3 个 Bash 辅助脚本语法检查失败。本运行环境没有 Android SDK，因此未宣称完成 Android assemble；需用户本机按既有 dev20 环境编译。

# Changelog

## 0.1.0-dev20 — 2026-09-19

- 媒体库顶部工具栏改为闲置圆形图标、聚焦后展开文字；排序按字段使用不同图标并叠加升/降序方向提示。
- 从媒体库 Hero 向下进入工具栏时先只移动焦点，额外抑制延迟 bring-into-view，避免整页先被上下推一下。
- 海报墙按服务端 `total` 建立虚拟槽位：未加载项先显示稳定空块，进入可视区后按页续载；移除焦点链中的“加载更多”按钮，避免焦点跳回排序栏。
- 首页 Hero 高度预留媒体库首行的预布局区域并提前预取同尺寸图片，Hero 向下使用单次边界滚动；轮播海报向下直接进入媒体库，不再绕到左侧继续播放。
- 首页继续播放按钮与右侧轮播海报底边对齐，轮播卡片移除额外年份脚注，降低焦点路径和布局抖动。
- 播放器 OSD 的 6 秒隐藏改为“最后一次操作后的空闲计时”，D-pad、按键、触摸/空鼠操作都会重置；播放器图标改成更实心的统一光学中心设计，重绘倍速、睡眠、传输等图标。
- 版本号更新为 dev20 / 20；继续保持 R8 与资源裁剪关闭，不升级 Compose、Media3 或其他核心依赖。

## 0.1.0-dev19 — 2026-09-19

- 媒体库固定标题与排序/字幕/视图工具合并成单行透明工具栏，压缩垂直占用并保留窄屏空间。
- 首页轮播与“我的媒体库”拆分为相邻 Lazy 项，增加首行海报预取、焦点交接锁定和单次平滑滚动，降低轮播向下切换的卡顿与二次补偿跳动。
- 性能设置新增“自动 / 1080p / 原生最高”界面显示模式偏好；诊断页分别展示应用窗口、当前显示模式和设备最高模式。
- 播放器控制重绘为轻量矢量/半透明玻璃风格，强化播放主按钮、主题色焦点环和快进/快退秒数提示，不引入实时模糊。
- 保持 R8 与资源裁剪关闭，延续 dev18 播放兼容策略；版本号更新为 dev19 / 19。

## 0.1.0-dev13 — 2026-09-18

- 浅色焦点改为双层柔和阴影 + 2.5dp 聚焦框并轻微抬升；深色保持无阴影、聚焦框加粗到 5dp。
- 媒体库/详情页去除左上返回入口；剧集“从头播放”与“继续播放”改为次级重播 / 主要播放两种视觉语义。
- 字体菜单保留系统默认与用户自定义上传；为规避字体再分发版权风险，APK 不内嵌第三方字体。
- 播放器按钮与控制布局重置，补充章节、字幕、音轨、演职员、睡眠、播放信息、片头/片尾跳过与下一集提示。
- 纵向 4K MP4 播放问题已修复，并由用户实机播放确认。


## 0.1.0-dev12 — 2026-09-18

- 浅色主题焦点改为柔和悬浮阴影，并为横向行/网格预留阴影安全边距；深色主题取消焦点阴影、描边加粗并改成更扁平的填色。
- 首页移除“查看详情/暂停轮播”两个冗余按钮，建立播放 → 快速续播 → 媒体库的纵向焦点路径；右侧轮播向左完整循环一轮后可再按左回到播放按钮。
- Media3 从 1.6.1 定向升级至 1.9.4；针对 dev11 实机记录的 MP4 错误 2000（UnexpectedLoaderException → IndexOutOfBoundsException）增加一次忽略 edit list 的有界兼容重试。
- 不改变 Emby、CloudDrive2、MediaIndex STRM 的鉴权与网络边界；真实故障 MP4 仍需电视复测。


## 0.1.0-dev11 — 2026-09-17

- 恢复适配深浅主题的焦点描边，首页轮播保留原样；轻微平移阴影与全局开关。
- 设置媒体库选择间距统一，首页续播胶囊收窄并增加向下浏览提示。
- 减少动画逐帧重组，首页推荐和媒体库首行预先组合，使用有界平滑滚动。
- 最新入库离开时收起并返回开头，上下进入时聚焦第一张；标题按钮完整露出下方卡片和留白。
- 原生播放器改为圆形图标控件、聚焦提示和轨道列表；增加不含地址与凭据的错误诊断。
- 播放错误 2000 暂缓排查，不列为已修复；验证和限制见 docs/UI-DEV11.md。

## 0.1.0-dev2 — 2026-09-17

这是源码更新，不代表 Android APK 已编译或电视已验证。

- 延续 dev1 工程，不重做或删除现有页面/来源；版本号升为 2。
- 原生顶部增加统一的胶囊导航及当前栏目高亮，沿用同一配色、焦点与间距。
- 音轨/字幕面板打开时移除下层播放控制的可聚焦入口；PlayerView 不截获 D-pad 焦点。真实遥控器仍待测试。
- 起播计时拆分为点击至首帧、源解析、Activity 交接和引擎首帧；重试/恢复不把后台等待误算为起播。引擎回调是真实首帧事件，当前没有实测成绩。
- 新增 12 个时序契约断言；独立 Kotlin 测试累计 114 项通过。
- 新增浏览器一次上传的源码初始化 + Android CI：归档校验、目标仓库限制、已有工程保护、标准 Wrapper、测试/lint/构建、APK 校验值和失败日志。
- 新增 18 个离线归档恢复安全测试，全部通过；工作流本身做离线验证，但没有执行远程建仓/提交/构建。
- 内嵌 GitHub 源码包不带评审图片、HTML 示意素材、任何真实凭据或签名密钥。

## 0.1.0-dev1 — 2026-09-17

初始原生源码、全套设计与 HTML 评审原型；Emby、CD2 WebDAV、STRM、安全策略和 Media3 初始实现。102 项纯 Kotlin 契约与 12 项 HTML 浏览器检查通过；没有 Android APK。原始状态存档于 `docs/history/STATUS-dev1.md`。
