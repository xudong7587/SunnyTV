# Module rules

Follow DESIGN_SYSTEM and offline preview. Preserve global vs single-library hierarchy and Emby native library images. Every action must be D-pad reachable. Never put networking or image decoding on the UI thread. Keep stable keys, return focus and modal focus isolation. Unimplemented controls must not pretend to work.

Read root AGENTS.md and docs/STATUS.md before changes.
