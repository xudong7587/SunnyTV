# Module rules

Current implementation is CD2 WebDAV, not native gRPC. Keep all file operations read-only. 207 is success for PROPFIND. Do not weaken XML entity/size/path checks. Native RPC must be generated from a version-verified proto in a separately tested adapter.

Read root AGENTS.md and docs/STATUS.md before changes.
