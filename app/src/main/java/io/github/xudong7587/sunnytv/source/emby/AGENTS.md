# Module rules

All Emby API/DTO/image behavior stays here. Keep user-level login and multi-source identity. Handle null fields and array Latest responses. DeviceProfile is a known P0 gap. Do not use NAS filesystem paths on Android; do not claim SupportsDirectPlay alone proves hardware support. Never extract provider cookies to the TV.

Read root AGENTS.md and docs/STATUS.md before changes.
