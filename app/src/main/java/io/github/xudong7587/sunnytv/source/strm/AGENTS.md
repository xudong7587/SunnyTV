# Module rules

Keep UTF-8/BOM/newline/size validation, a single HTTP(S) target, recursion and cycle limits. Do not read extensionless MediaIndex streams as text. Do not re-encode signed URL queries. Use the same scoped HTTP transport and UA as playback.

Read root AGENTS.md and docs/STATUS.md before changes.
