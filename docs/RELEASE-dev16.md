修复海报墙、文件夹、设置和详情页的遥控上下焦点阻断，统一网格跨行与分页返回。

- 首页各媒体库可切换最新入库、最新上映、随机，以服务端排序更新媒体。
- 取消按钮聚焦上移和缩放，减轻阴影，细化描边，对齐首页内容。
- Banner 居中裁切铺满，剧集图片使用同心圆角。
- 手机竖屏视频默认竖屏播放，TV 保持横屏。
- 播放控制更紧凑，新增 1x / 1.25x / 1.5x / 2x 倍速及底部退出，子菜单返回恢复原按钮焦点。

发布前验证：Android 编译、单元测试、lint、Android 模拟器交互测试，以及固定 dev14 签名一致性和保留数据的覆盖升级。签名与升级报告、源码提交和 APK SHA256 随安装包附带。

这是开发测试版；模拟器验证不代表实体电视验收，真实视频的方向切换、倍速同步和设备兼容性仍需实机反馈。
