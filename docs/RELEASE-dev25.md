焦点外框、阴影留白与手机/平板间距的一轮细节修复，并补上玩家反馈的返回 banner 卡顿。

- 聚焦外框改为与卡片轮廓同心的内缩描边（圆角半径随内缩一起减小），圆角处不再露出海报细缝。
- 首页与详情页、云盘目录、媒体库工具栏的行内留白统一为左右 14dp / 上 12dp / 下 18dp，最左媒体块与媒体库卡片的柔和阴影不再被行分区裁掉，标题与首张卡左边缘保持 30dp 不变。
- 媒体库（含文件夹视图）从媒体区按「上」回 banner 改为一次受控滚动，修掉中途停住再起步的卡顿；逐帧位移由 `99,62,35,…,0,0,173,296` 变为单段减速。
- 手机 / 平板媒体库与文件夹视图里 banner 到媒体区的间距由 122dp 收到 40dp（电视端取值逐值不变）。

开发期间的私有构建曾临时内置几个第三方字体用于自用验证，本仓库与发布包均不含任何字体文件：
界面与字幕字体只提供「系统默认」和「用户自定义上传」（支持 TTF / OTF / TTC，文件保存在本机）。
`scripts/check-project.py` 会直接拒绝 `app/src/main` 下出现 `.ttf/.otf/.ttc`。

发布前验证（工作流硬门槛）：`scripts/test-bootstrap.py`、`scripts/check-project.py`（含"不打包任何字体文件"检查）、
Android 编译、单元测试、`lintDebug`（本版把 dev22 起累积的 14 项 lint 报错一并修掉，现 0 错误）、
APK 签名有效且与仓库现有签名 Secret 的公开证书一致，以及 APK 包名/版本号断言。
签名报告、源码提交与 APK SHA256 随安装包附带。不替换既有标签或资产，不生成替代密钥。

模拟器交互测试本轮**不作为发布门槛**：它照常执行并把完整报告作为构建产物上传，但其中 8 项断言落后于
已被验收的界面行为（清单与原因见 `docs/KNOWN-ISSUES.md`，均为 dev22 期起就存在、本轮未新增）。
没有跳过或删除任何测试，也没有用跳过测试的方式制造全绿。

已知限制：模拟器验证不代表实体电视与手机验收；真实设备的遥控手感、手机 / 平板触摸手感与真实媒体播放仍需实机反馈。
