# dev3 本机验证记录（2026-09-17）

代码提交：`6f9c55a`（主题与偏好）、`3198fb8`（Emby 数据）、`6cdd909`（原生界面、播放器与预览）。

## 已实现并编译

- 首页整屏背景与六卡手风琴轮播，左右循环；焦点进入、页面离开、弹窗、忙碌和减少动画时暂停自动切换。
- 设置支持随机推荐的全部/部分媒体库、切换秒数，以及深浅主题和十组重点色。
- 顶部胶囊导航、图标按钮聚焦展开中文；逐库最新十项，普通行末尾查看全部且不循环。
- 单库保留顶部推荐，下面为海报墙；排序、字幕偏好、Poster/Thumb/Banner 和文件夹行模式。
- 详情页海报取色、媒体 Logo、版本/音轨/字幕、已看/收藏、演员和相似推荐。所有请求仍由适配层执行。
- 播放器字幕偏好和轨道选择；设备诊断读取真实显示模式。窗口只请求当前分辨率下已公布的最高刷新率。
- 手机普通 Launcher 图标与 TV Launcher 入口并存。
- 固定电脑预览脚本 `scripts/start-tv-preview.ps1`，1920×1080/320 独立显示，开启 keep-active/stay-awake，退出保留日志。

## 实际验证

- 本机 Wrapper：`assembleDebug assembleDebugAndroidTest testDebugUnitTest lintDebug` 成功。
- JUnit 13 个测试方法通过，其中 ContractSuite 包含 114 项核心契约检查，HTTP 测试合计 8 个。
- lint：0 errors、27 warnings，未关闭 lint 或跳过失败项。
- Android 16 手机运行 3 项 Compose 交互测试通过：轮播循环与反向、列表末端停止、聚焦中文与图标可访问名称。
- 首次设备测试因窗口失去焦点/手机触摸输入模式未完成；测试显式使用键盘输入模式并保持屏幕亮起，应用前台重跑得到 `OK (3 tests)`。不将此前失败算作通过。
- 新版已安装，系统查询确认普通 Launcher 可解析 MainActivity，版本 `0.1.0-dev3` / code 3。
- 电脑虚拟显示成功创建，日志确认 1920×1080，60 Hz。截图确认设置/添加来源页面正常展示。

APK：`app/build/outputs/apk/debug/app-debug.apk`

SHA-256：`74ac69364b09803f1ca44b4c2ca124d3bacfdaef6291ec97f1ac26e3d626ac94`

使用本机持久 debug key 签名，未提交密钥。已按用户明确授权卸载原签名旧版并清除旧配置；此次 dev3 更新可覆盖安装。

## 尚未验证/限制

- 未完成真实媒体库所有页面视觉验收、实际视频/多版本音字幕联调；MockWebServer 测试不代表用户服务兼容。
- 各服务版本可能不支持全部排序键，需联调确认；不宣称所有排序已获服务端验证。
- 电脑预览已加入防休眠并成功启动，尚不能证明长时间闲置问题彻底消除。
- 虚拟显示为 60 Hz；未验证电视 4K/144 Hz、帧时间、HDR/硬解或真实首帧延迟。
- 原有自动下一集、字幕延迟、默认音轨、CD2 原生 API 等未实现项仍见 STATUS。
- 本轮没有修改 MediaIndex、NAS 或真实服务凭据。

本机详细日志位于忽略目录 `.ci-logs`；其中可能含设备信息，不纳入仓库。
