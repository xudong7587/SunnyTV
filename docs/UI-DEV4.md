# dev4：手机适配与反馈修复（2026-09-17）

代码提交：`264eac7`（Emby 状态写入与日志排查）、`b588532`（手机布局、品牌和焦点）。

## 已实现

| 用户反馈 | 本轮代码处理 |
|---|---|
| 顶部导航下方的不透明条 | 移除根容器顶部裁切，单库背景铺到顶部；内容单独留出导航空间 |
| Emby 连续事件通知 | 根据日志确认状态写请求与 Webhook 爆发；禁止整剧/整季批量已看，单项确认，拦截确认键长按重复；详见排查记录 |
| 顶部方向键无法进入正文 | 导航 Down 显式指向正文焦点组；未被 Compose 消费的方向键通过公开 onKeyDown 转入键盘焦点模式 |
| 媒体库说明文案 | 删除“使用 Emby 原生媒体库图片” |
| 圆形图标和中文 | 未展开 Action 固定 48×48 dp，聚焦/选中展开中文；升降序有不同箭头，确认采用对勾 |
| 排序与文件夹按钮 | 保留服务器端升降序查询，增加明显状态；电影库隐藏文件夹模式，仅电视节目、综合/家庭视频及文件夹显示 |
| 首次连接和 CD2 | 等本地来源读取完成后，无 Emby 时展示连接表单；导航、来源添加与关于中的 CD2 入口暂时隐藏，底层适配保留 |
| 确认按钮不明确 | “确认”“保存”“知道了”等使用对勾图形 |
| 轮播媒体库选项层级 | 全部库开关下展开缩进、独立底板的子菜单 |
| 已看/收藏反馈 | 解析服务端返回状态并同步已加载列表；选中态明确显示；失败弹出提示 |
| 手机竖屏与轮播 | 主 Activity 解除横屏锁定；窄屏导航、设置和首页改为适配布局；固定方形主卡，其他细条循环移动并可横向滚动，卡片下显示年份 |
| 新 Logo | 依据用户确认的太阳＋播放三角图，重建可缩放矢量图形；桌面、TV banner 与深浅主题页头共用核心图形；播放页仍显示媒体自身 Logo |

## 实际验证与交付

- 本机 `assembleDebug testDebugUnitTest lintDebug assembleDebugAndroidTest` 成功（最终日志 `mobile-build5.log`）。
- 17 个 JUnit 方法通过：1 个核心套件（114 项检查）、8 个 Emby API 测试、4 个表现规则测试、4 个传输测试。
- lint：0 errors、27 warnings；未关闭规则或跳过测试。
- 7 项设备交互测试已编译。设备测试曾被正常应用重新打开打断，未得到本轮通过结果；用户明确要求“先继续开发，稍后再测手机”，因此停止设备测试。不能沿用 dev3 的 3 项通过来声称 dev4 已验证。
- dev4 最终 APK 尚未覆盖安装；本轮较早安装过中间构建。最终版本应以此文件与校验值为准。

APK：`dist/SunnyTV-0.1.0-dev4-debug.apk`（忽略目录，未提交二进制）。

SHA-256：`7cced3b51b567d88e193c22353f37c8a83f800a5cb99efcd6df1efe96d45219e`

版本：`0.1.0-dev4` / versionCode 4；使用同一本机 debug key。

## 待设备复测

手机竖屏与横屏布局、TopNav → 正文方向键、轮播固定主卡的触摸/方向键行为、弹窗返回焦点、首次连接键盘遮挡、实际库排序与收藏状态。测试先用不恢复真实账号的中立宿主；通知原因复测不得随意修改真实媒体记录。

另一路 Webhook 502 与 MediaIndex 事件转发策略未改动；不能宣称服务器端通知问题已彻底解决。详见 [日志排查](NOTIFICATION-INCIDENT-20260917.md)。4K/144 Hz、真实视频解码和长时间闲置验证继续保留为未完成。
