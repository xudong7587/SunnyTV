# dev9：电视来源表单输入与焦点修复

- 为来源名称、地址、用户名和密码处理 IME Next/Done（含输入法提供的其他确认 action），按明确焦点链进入下一项；密码确认进入保存按钮，不直接发起登录。
- 每项提供始终显示的“下一项”或“填写完成”按钮。遥控器向下可达确认按钮，确认后进入下一字段；向上返回上一字段。
- 用户名和密码改为纵向排列，表单保留滚动与输入法避让。“验证并保存”始终显示文字。
- 同时覆盖首次连接和设置页添加来源，不变更服务端登录或凭据存储逻辑。

验证：testDebugUnitTest、lintDebug、assembleDebug、assembleDebugAndroidTest 全部通过。28 个 JUnit 方法通过；lint 0 错误、26 警告。新增两项仪器测试覆盖首次连接 IME 逐项确认、添加来源遥控器确认及保存入口；总计13项交互测试编译但未执行。当前 adb 无连接设备，不能声称电视验收通过。

APK versionCode 9，沿用本机 debug 签名，校验值见发布附件 SHA256SUMS.txt。
