# 本地样本规范

自动测试使用 `.test` 域名、虚构 Token 和 MockWebServer；这些都不是可播放资源。

真机联调样本由用户在本地填写，不放进此目录。STRM内容和截图可能包含有权限的播放令牌与私人库信息；共享前脱敏。

建议准备：H264-MP4、HEVC-MKV、多音轨+文本字幕、MP4尾部metadata、MediaIndex302、MediaIndexproxy、CD2视频和CD2STRM。按TEST_MATRIX逐条记录，不把一份文件通过等同于所有格式通过。
